(() => {
    const app = JSON.parse(document.getElementById('app-data').textContent);
    const chat = document.getElementById('vendor-ai'); const launcher = document.getElementById('vendor-ai-launcher');
    const messages = document.getElementById('vendor-ai-messages'); const input = document.getElementById('vendor-ai-input'); const form = document.getElementById('vendor-ai-form'); const typing = document.getElementById('vendor-ai-typing'); const status = document.getElementById('vendor-ai-status');
    const id = (() => { const key = 'ld_vendor_ai_conversation'; let value = localStorage.getItem(key); if (!value) { value = crypto.randomUUID(); localStorage.setItem(key, value); } return value; })();
    const escape = value => String(value || '').replace(/[&<>'"]/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;' }[c]));
    const render = history => { messages.innerHTML = history.length ? history.map(item => `<div class="vendor-ai-message ${item.role === 'user' ? 'is-user' : ''}">${escape(item.content)}</div>`).join('') : '<div class="vendor-ai-message">¡Hola! Soy el Vendedor IA. ¿En qué te ayudo?</div>'; messages.scrollTop = messages.scrollHeight; };
    const refresh = async () => { try { const data = await fetch(`${app.api_url}?action=ai_public_history&conversation=${encodeURIComponent(id)}`).then(r => r.json()); render(data.history || []); status.textContent = data.status?.connected ? '● IA conectada' : '● IA desconectada'; status.classList.toggle('offline', !data.status?.connected); } catch (_) { status.textContent = '● IA desconectada'; } };
    launcher.addEventListener('click', () => { chat.dataset.state = 'open'; launcher.hidden = true; input.focus(); });
    document.querySelector('[data-vendor-ai-minimize]').addEventListener('click', () => { chat.dataset.state = 'minimized'; launcher.hidden = false; });
    document.querySelector('[data-vendor-ai-close]').addEventListener('click', () => { chat.dataset.state = 'closed'; launcher.hidden = false; });
    form.addEventListener('submit', async event => { event.preventDefault(); const text = input.value.trim(); if (!text) return; render([...Array.from(messages.children).map(node => ({ role: node.classList.contains('is-user') ? 'user' : 'assistant', content: node.textContent })), { role:'user', content:text }]); input.value = ''; typing.hidden = false; try { const response = await fetch(app.api_url, { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({ action:'ai_public_chat', conversation:id, message:text }) }); const data = await response.json(); if (!data.ok) throw new Error(data.error); await refresh(); if (data.reply?.human_help) { messages.insertAdjacentHTML('beforeend', `<a class="vendor-ai-human" target="_blank" rel="noopener" href="https://wa.me/5493415699338?text=${encodeURIComponent(data.reply.human_help.message)}">${escape(data.reply.human_help.message)} Consultar a Allessandra</a>`); messages.scrollTop = messages.scrollHeight; } } catch (error) { messages.insertAdjacentHTML('beforeend', `<div class="vendor-ai-message">${escape(error.message || 'No pude responder.')}</div>`); } finally { typing.hidden = true; } });
    let start; document.querySelector('[data-vendor-ai-drag]').addEventListener('pointerdown', event => { if (event.target.closest('button')) return; start = { x:event.clientX, y:event.clientY, left:chat.offsetLeft, top:chat.offsetTop }; event.currentTarget.setPointerCapture(event.pointerId); }); document.querySelector('[data-vendor-ai-drag]').addEventListener('pointermove', event => { if (!start) return; chat.style.right = 'auto'; chat.style.bottom = 'auto'; chat.style.left = `${Math.max(0, start.left + event.clientX - start.x)}px`; chat.style.top = `${Math.max(0, start.top + event.clientY - start.y)}px`; }); document.querySelector('[data-vendor-ai-drag]').addEventListener('pointerup', () => { start = null; });
    refresh();
})();
